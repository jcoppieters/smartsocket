
<script>
  function setStatus(inx, state, value) {
    if (typeof state === "boolean") state = (state) ? "Y" : "N";
    ajax("http://<%= req.headers.host %>/switches/set/" + inx + "?state="+state+"&value="+value, function(data){ 
    });
  }
</script>